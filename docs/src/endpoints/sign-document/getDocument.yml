tags:
  - Sign Document (RALA)
summary: "Get the latest signed document"
description: "Get the latest signed document"
operationId: "getSignedDocument"
security:
  - JWT: [user]
responses:
  200:
    description: "Latest Document"
    content:
      application/json:
        schema:
          type: object
          properties:
            data:
              $ref: "../../schemas/SignedDocument.yml"
            status:
              type: string
              enum: ['success', 'error']
              default: 'success'
              description: Status for overall request
  401:
    $ref: "../../errors/http401.yml"
